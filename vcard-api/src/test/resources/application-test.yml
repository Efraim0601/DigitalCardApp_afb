spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/vcard_test
    username: vcard
    password: vcard
  jpa:
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false

app:
  admin:
    email: test@test.com
    password: test
    session-secret: test-secret
  cors:
    allowed-origins: http://localhost:4200
  card:
    fixed-phone: "222 233 068"
    fixed-fax: "222 221 785"
